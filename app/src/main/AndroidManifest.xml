<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="moe.shizuku.manager.permission.API_V23" />
 <service android:name=".SpeechService" android:exported="false" />
<service android:name=".UserServiceImpl" android:exported="false" />

    <application
        android:name=".UltronApplication"
        android:allowBackup="true"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar"
        tools:targetApi="34">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Foreground service for speech recognition -->
        <service
            android:name=".SpeechService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <!-- Shell-privileged service for Shizuku bindUserService -->
        <service
            android:name=".UserServiceImpl"
            android:enabled="true"
            android:exported="false"
            android:permission="moe.shizuku.manager.permission.API_V23" />

        <!-- Panic button broadcast receiver -->
        <receiver
            android:name=".PanicReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.ultron.agent.PANIC" />
            </intent-filter>
        </receiver>

    </application>

</manifest>

